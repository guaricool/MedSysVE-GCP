const { GoogleAuth } = require('google-auth-library'); const { GoogleGenAI } = require('@google/genai'); async function test() { const auth = new GoogleAuth({ scopes: 'https://www.googleapis.com/auth/cloud-platform' }); const client = await auth.getClient(); const token = await client.getAccessToken(); const ai = new GoogleGenAI({ httpOptions: { headers: { Authorization: 'Bearer ' + token.token } }, vertexai: { project: 'medsysve-gcp', location: 'us-central1' } }); const response = await ai.models.generateImages({ model: 'imagen-3.0-generate-001', prompt: 'a beautiful modern medical clinic', config: { numberOfImages: 1, outputMimeType: 'image/jpeg' } }); console.log(response.generatedImages[0].image.imageBytes.substring(0,20)); } test().catch(console.error);
